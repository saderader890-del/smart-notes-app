import React from 'react';
import ReactDOM from 'react-dom/client';
import SmartNotesApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SmartNotesApp />
  </React.StrictMode>
);
